server:
  port: 51603
spring:
  application:
    name: leadnews-admin-gateway
  cloud:
    nacos:
      discovery:
        server-addr: ${VPS_HOST}:8848
      config:
        server-addr: ${VPS_HOST}:8848
        file-extension: yaml
        group: leadnews