server:
  port: 51602
spring:
  application:
    name: leadnews-wemedia-gateway
  cloud:
    nacos:
      discovery:
        server-addr: ${VPS_HOST}:8848
      config:
        server-addr: ${VPS_HOST}:8848
        file-extension: yaml
        group: leadnews