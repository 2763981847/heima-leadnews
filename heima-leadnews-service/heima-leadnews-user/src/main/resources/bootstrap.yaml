server:
  port: 51801
spring:
  application:
    name: leadnews-user
  cloud:
    nacos:
      discovery:
        server-addr: ${VPS_HOST}:8848
      config:
        server-addr: ${VPS_HOST}:8848
        file-extension: yaml
        group: leadnews