server:
  port: 51805
spring:
  application:
    name: leadnews-admin
  cloud:
    nacos:
      discovery:
        server-addr: ${VPS_HOST}:8848
      config:
        server-addr: ${VPS_HOST}:8848
        file-extension: yaml
        group: leadnews