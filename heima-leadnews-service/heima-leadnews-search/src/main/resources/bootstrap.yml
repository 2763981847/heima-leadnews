server:
  port: 51804
spring:
  application:
    name: leadnews-search
  cloud:
    nacos:
      discovery:
        server-addr: ${VPS_HOST}:8848
      config:
        server-addr: ${VPS_HOST}:8848
        file-extension: yaml
        group: leadnews